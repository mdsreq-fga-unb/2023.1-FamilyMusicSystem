{
  "kind": "collectionType",
  "collectionName": "lessons",
  "info": {
    "singularName": "lesson",
    "pluralName": "lessons",
    "displayName": "Lesson",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "Horary": {
      "type": "datetime",
      "required": true,
      "default": "2023-01-01T15:00:00.000Z"
    },
    "Teacher": {
      "type": "enumeration",
      "enum": [
        "teacher"
      ],
      "required": true
    },
    "Student": {
      "type": "enumeration",
      "enum": [
        "student"
      ],
      "required": true
    },
    "Room": {
      "type": "enumeration",
      "enum": [
        "room"
      ],
      "required": true
    }
  }
}
